node_modules/
dist/
build/
.DS_Store
*.local

# Pre-built circuit keys (generated by `bun run build:cache`).
# These can be 100s of MB — rebuild after any circuit source change.
ui/public/circuit-cache/

# Deployment output — contains contract private keys. Never commit this.
contracts/deploy-output.json
