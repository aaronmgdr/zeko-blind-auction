{
  "name": "blind-auction-contracts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json",
    "test": "bun test.ts",
    "test:fresh": "bun run build && bun test.ts",
    "build:cache": "bun run build && bun scripts/buildCache.ts",
    "deploy": "bun run build && bun scripts/deploy.ts"
  },
  "dependencies": {
    "o1js": "^2.0.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.4.0"
  }
}
